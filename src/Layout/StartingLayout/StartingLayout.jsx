import { Outlet } from "react-router-dom";

const StartingLayout = () => {
    return (
        <div>
            <Outlet></Outlet>
            
        </div>
    );
};

export default StartingLayout;